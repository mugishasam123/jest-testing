function stringthLength(string){
    
   if(string.length>=1 && string.length<=10){
       return string.length;
   }
   else{
       throw('it can work')
   }
    }
  
    module.exports = stringthLength;
   